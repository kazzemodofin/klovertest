<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <com.kazeem.spinningwheel.presentation.spinner.SpinnerView
        android:id="@+id/wv_main_wheel"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <androidx.constraintlayout.widget.Guideline
        android:id="@+id/guideline"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintGuide_begin="0dp"
        android:orientation="horizontal" />

    <ImageView
        android:id="@+id/iv_arrow"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:src="@drawable/wheel_ticker"
        app:layout_constraintStart_toStartOf="@+id/wv_main_wheel"
        app:layout_constraintEnd_toEndOf="@+id/wv_main_wheel"
        app:layout_constraintTop_toTopOf="@+id/guideline"
        android:contentDescription="@string/app_name" />

</androidx.constraintlayout.widget.ConstraintLayout>